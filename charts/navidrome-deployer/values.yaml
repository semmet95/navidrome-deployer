# Application settings
namespace: navidrome-system
image:
  repo: "deluan/navidrome"
  tag: "0.58.5"
  pullPolicy: IfNotPresent
containerPort: 4533
resources:
  limits:
    memory: 1Gi
  requests:
    cpu: 500m

# Service configuration
service:
  type: LoadBalancer
  port: 80

# Public endpoints configuration
baseDomain: "example.com"
clusterissuer:
  email: singhamitch@outlook.com
  name: letsencrypt-prod
certificate:
  secret: navidrome-cert

filebrowser:
  imageURI: "filebrowser/filebrowser:v2.59.0"
  reconfigImageUri: "ghcr.io/semmet95/navidrome-deployer/filebrowser-reconfig:0.19.0"
  containerPort: 80
  resources:
    limits:
      memory: 512Mi
    requests:
      cpu: 500m
  service:
    port: 80